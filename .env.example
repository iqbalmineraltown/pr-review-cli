# ═══════════════════════════════════════════════════════════════
# PR Review CLI - Bitbucket Configuration
# ═══════════════════════════════════════════════════════════════
#
# Setup Instructions:
# 1. Copy this file to ~/.pr-review-cli/.env
#    mkdir -p ~/.pr-review-cli
#    cp .env.example ~/.pr-review-cli/.env
# 2. Create an API Token at: https://bitbucket.org/account/settings/api-tokens/
# 3. Fill in the required fields below
#
# Required API Token Permissions:
#   ✅ Pull requests: Read
#   ✅ Repositories: Read
#   ✅ Account: Read (optional, for displaying your name)
# ═══════════════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────────────────────
# REQUIRED CONFIGURATION
# ────────────────────────────────────────────────────────────────────────

# Your Bitbucket email (for authentication)
PR_REVIEWER_BITBUCKET_EMAIL=your_email@example.com

# Your Bitbucket API Token (from https://bitbucket.org/account/settings/api-tokens/)
PR_REVIEWER_BITBUCKET_API_TOKEN=your_api_token_here

# Your default Bitbucket workspace (optional, but recommended)
# This allows you to run: pr-review review (without specifying workspace)
PR_REVIEWER_BITBUCKET_WORKSPACE=your_workspace_here

# ────────────────────────────────────────────────────────────────────────
# OPTIONAL CONFIGURATION
# ────────────────────────────────────────────────────────────────────────

# Path to Claude CLI (default: "claude")
# CLAUDE_CLI_PATH=/custom/path/to/claude

# Bitbucket API base URL (default: https://api.bitbucket.org/2.0)
# BITBUCKET_BASE_URL=https://api.bitbucket.org/2.0

# Cache directory (default: ~/.pr-review-cli/cache)
# CACHE_DIR=/custom/cache/directory
